搜索文本文件件
分词
建立索引

UML建模
关键字截取，按单词截取
操作符 AND OR NOT

数据结构：字典树、哈希表、二分搜索平衡树。

关键词解析：以空格分开的单词，忽略小横线-，截断单引号'和其它非数字和字母字符。
每个关键字有什么文件。
每个文件有多少个关键字，各有多少个。

每个关键字信息：关键字ID，文件列表（用位记录）。
文件信息：路径、关键字，关键字数量，trie存储。

关键字用trie，每个关键字要记录文件列表，文件列表用链表表示。

搜索单个关键字时，把文件列表列出。

大约50个txt文档，
